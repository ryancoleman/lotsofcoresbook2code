#
# Recurse down into lib (to build the library)
#                   includes (to install includes)
#
SUBDIRS = other_libs lib include bin examples doc

DISTCHECK_CONFIGURE_FLAGS = @DISTCHECK_CONFIGURE_FLAGS@
